Javascript에서는 함수도 인자로 보낼 수 있다. 
인자로 보내면 바로 함수 호출 가능
 data3가 이미 함수이므로 바로 data3(data1,data2)로 호출가능하다.
==========================================================================================================================================
의존성이 없다, 결합도가 낮다->다른 얘는 고쳐지는데 나는 고칠 필요없다.
==========================================================================================================================================
 var -> local value를 의미
 var를 안쓰면 global value로 변함. 
==========================================================================================================================================
function getAdd(num1,num2)
{
  var result = num1+num2;
  return function()
  {
    console.log(result);
  }
}
이거 호출하는 방법은
A=getAdd(num1,num2);
A();
이렇게 시행해야함
=========================================================================================================================================
(function()
    {
      var a=3;
      function getA()
      {
        return a;
      }
      console.log(getA());
    })();
    console.log(getA());
이러면  console.log(getA());에서 getA()참조가 안됨. 
 {
      var a=3;
      function getA()
      {
        return a;
      }
      console.log(getA());
    }
여기 파트는 이름 없는 function()에 의해 보호받고 있어서 참조 불가.
=========================================================================================================================================
(function dummy()
{
	Console.log("name");
})();
->이렇게 할 경우 ()안의 있는 것을 실행 시켜줘라는 의미.
=========================================================================================================================================
HTML 코딩한 것
 h1은 제목 1이라는 의미
<div>
 <h1>안녕 HTML!</h1>
 <p>HTML은 웹프론트엔드에서 구조를 담당합니다.</p>
 <A href="http://www.w3schools.com/TegS/">HTML Tags</A>
 </div>
 =========================================================================================================================================
